import { Frame } from "../frame";
declare class Session extends Frame {
    constructor(isdev: boolean);
    Inject(): void;
    _init(): void;
    _cookieSet(session_id: string, start_time: string, domain?: string): void;
    _event(): void;
}
declare let DefaultSessionPlugin: Session;
export { Session, DefaultSessionPlugin };
