declare abstract class Frame {
    protected isdev: boolean;
    constructor(isdev: boolean);
    host(): string;
    event_url(): string;
    device_url(): string;
    ajax(params: any): void;
    convertData(data: any): any;
}
export { Frame };
